package com.slemarchand.serverbanner;

import java.util.List;

import org.junit.Assert;
import org.junit.Test;

public class ConfigurationFactoryTest {
	
	/*
	@Test
	public void testToStringList() {
		
		String[] array = {"hello", "bonjour"};
		
		List list = new ConfigurationFactory().toStringList(array);
	
		Assert.assertEquals(array.length, list.size());
		Assert.assertEquals(array[0], list.get(0));
		Assert.assertEquals(array[1], list.get(1));
	}
	*/	
}
